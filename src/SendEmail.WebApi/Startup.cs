using Microsoft.AspNetCore.Builder;
using Microsoft.AspNetCore.Hosting;
using Microsoft.Extensions.Configuration;
using Microsoft.Extensions.DependencyInjection;
using Microsoft.Extensions.Hosting;
using Microsoft.OpenApi.Models;
using SendEmail.FluentEmailAdapter;
using SendEmail.FluentEmailAdapter.Microsoft.Extensions.DependencyInjection;
using SendEmail.WebApi.Extensions;

namespace SendEmail.WebApi
{
    public class Startup
    {
        public IConfiguration _configuration { get; }

        public Startup(IConfiguration configuration)
        {
            _configuration = configuration;
        }

        // This method gets called by the runtime. Use this method to add services to the container.
        public void ConfigureServices(IServiceCollection services)
        {
            services.AddControllers();
            services.AddSwaggerGen(c =>
            {
                c.SwaggerDoc("v1", new OpenApiInfo { Title = "SendEmail.WebApi", Version = "v1" });
            });

            var fluentEmailAdapterConfiguration = _configuration.SafeGet<FluentEmailAdapterConfiguration>();
            fluentEmailAdapterConfiguration.UserName = _configuration["FluentEmailAdapterConfiguration.UserName"];
            fluentEmailAdapterConfiguration.PassWord = _configuration["FluentEmailAdapterConfiguration.PassWord"];

            services.AddFluentEmailAdapter(fluentEmailAdapterConfiguration);
        }

        // This method gets called by the runtime. Use this method to configure the HTTP request pipeline.
        public void Configure(IApplicationBuilder app, IWebHostEnvironment env)
        {
            if (env.IsDevelopment())
            {
                app.UseDeveloperExceptionPage();
                app.UseSwagger();
                app.UseSwaggerUI(c => c.SwaggerEndpoint("/swagger/v1/swagger.json", "SendEmail.WebApi v1"));
            }

            app.UseHttpsRedirection();

            app.UseRouting();

            app.UseAuthorization();

            app.UseEndpoints(endpoints =>
            {
                endpoints.MapControllers();
            });
        }
    }
}
